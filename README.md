# ego
 Chrome extension that lets you describe a task in plain English and automatically performs it on any webpage. It takes a screenshot, uses an AI vision model to identify where to interact, maps it to the exact DOM element, executes the action, and verifies something changed, repeating until the task is done.
